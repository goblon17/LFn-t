lina 18:
#define isPressed(x) (digitalRead(x)==LOW)

makro do prostego sprawdzenia stanu przycisku (wykorzystujemy wiedzę o sposobie podłączenia)
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
linia 186:
while ( (valP1 = digitalRead(buttonP1)) == HIGH || (valP2 = digitalRead(buttonP2)) == HIGH ) {}

oczekiwanie na naciśnięcie poprawnego przycisku przez którego kolwiek z graczy i pamiętanie który gracz nacisnął podany przycisk
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
linie 107 - 111:
char buf[4];
itoa(roundsNumber, buf, 10);
int pos = 8 - (strlen(buf) / 2);
lcd.setCursor(pos, 1);
lcd.printstr(buf);

wypisywanie numeru o długości do 3 cyfr na środku drugiego rzędu wyświetlacza
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------